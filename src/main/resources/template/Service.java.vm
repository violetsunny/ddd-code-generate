package ${package}.${moduleName}.application;

import ${package}.${moduleName}.domain.model.entity.${className};
import ${package}.${moduleName}.domain.model.types.${className}Id;

import java.util.List;

/**
 * ${comments}Service
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Service {

    /**
     * 添加
     *
     * @param ${pathName}
     */
    boolean add(${className} ${pathName});

    /**
     * 更新
     *
     * @param ${pathName}
     */
    boolean update(${className} ${pathName});

    /**
     * 删除
     *
     * @param ids
     */
    boolean delete(List<${className}Id> ids);

    /**
     * 禁用
     *
     * @param id
     */
    boolean disable(${className}Id id);

}


